"""Reserved for model artifacts (e.g. Ollama) in a later phase.

Currently unused — the engine runs deterministic rules. This package will
host prompt templates and/or model backends when LLM integration lands.
"""

from __future__ import annotations
